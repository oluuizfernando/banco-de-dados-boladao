SELECT 1 + 1 AS MATH

SELECT  1 + 3 AS SOMA,
        5 - 10 AS [SUB(-1)],
        1700 + (-900) AS NEG,
        5 * (5 + 1) AS MULT_SOM,
        -5 * 500 / 40 % 3 AS TODO

SELECT
    1 + '1' AS SOMA1, -- CONVERTE PARA INT
    '1' + 1 AS SOMA2, -- CONVERTE PARA INT
    '1' + '1' AS SOMA3 -- CONCATENAÇÃO

SELECT
    1 + '1A'